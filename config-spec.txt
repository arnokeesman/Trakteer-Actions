### default
:offline
function {supporter_name}.mcfunction
say {supporter_name} donated {amount}
say something else

### kaboom
:if support_message contains boom
:if amount gte 10000
:include default

execute at {receiver} run summon creeper

### splash
:if support_message contains splash

execute at {receiver} run setblock ~ ~ ~ water
